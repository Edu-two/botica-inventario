// Controlador de Productos - manejará las operaciones CRUD
const db = require("../config/db");

// Aquí van las funciones reales después (listar, agregar, actualizar, eliminar)
exports.obtenerProductos = (req, res) => {
  res.send("Función obtenerProductos aún no implementada");
};

exports.agregarProducto = (req, res) => {
  res.send("Función agregarProducto aún no implementada");
};
